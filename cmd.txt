What percentage of calls in call details have a matching record in CallTrak, and what percentage have a matching transcript?
How many answered calls from yesterday have no CallTrak record?
How many answered calls have no transcript?
Are there transcripts or CallTrak records that have no matching call in call details?
How many records in each view have a missing call ID or member ID?
this is the config
 
CREATE OR REPLACE STORAGE INTEGRATION AZURE_STGEHAEDP_STR_INT

  TYPE = EXTERNAL_STAGE

  STORAGE_PROVIDER = AZURE

  ENABLED = TRUE

  AZURE_TENANT_ID = 'b7c537d8-4835-4261-b3ad-6a6d8cbb84b2'

  USE_PRIVATELINK_ENDPOINT = TRUE

  STORAGE_ALLOWED_LOCATIONS = (

      'azure://stgehaedpdev01.blob.core.windows.net/landing/'

  );  
 
CREATE or replace STAGE BRONZE_DB.EDL_BRONZE_ADLS.AZURE_STGEHAEDP_STG

URL = 'azure://stgehaedpstg01.blob.core.windows.net/landing/'

STORAGE_INTEGRATION = AZURE_STGEHAEDP_STR_INT;
 
file format wasn't added
 
this was existing so it was already able to list
 
but you can login and do a ls
 
 
